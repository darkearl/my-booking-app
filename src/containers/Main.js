import React, {Component, PropTypes} from 'react';
import {Text} from 'react-native';
export default class Main extends Component {
  static propTypes = {
  }

  render() {
    return <Text>Trang Main</Text>
  }
}